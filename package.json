{
  "name": "vite-project",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env NODE_ENV=development ts-node --project ./scripts/tsconfig.json ./scripts/dev.ts --esbuild --vite",
    "dev:tsc": "cross-env NODE_ENV=development ts-node --project ./scripts/tsconfig.json ./scripts/dev.ts --tsc --vite",
    "preview": "yarn run build && cross-env NODE_ENV=production electron ./dist/main",
    "dev:renderer": "vite",
    "dev:main": "cross-env NODE_ENV=development ts-node --project ./scripts/tsconfig.json ./scripts/dev.ts --esbuild",
    "dev:main:tsc": "cross-env NODE_ENV=development ts-node --project ./scripts/tsconfig.json ./scripts/dev.ts --tsc",
    "build": "yarn run clean && vite build && tsc --project ./src/main/tsconfig.json",
    "clean": "ts-node --project ./scripts/tsconfig.json ./scripts/clean.ts",
    "pack": "yarn run build && yarn run rebuild && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder -c ./electron-builder.yml --win --linux",
    "pack:all": "yarn run build && yarn run rebuild && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder -c ./electron-builder.yml --win --linux --mac",
    "pack:win": "yarn run build && yarn run rebuild && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder -c ./electron-builder.yml --win",
    "pack:mac": "yarn run build && yarn run rebuild && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder -c ./electron-builder.yml --mac",
    "pack:linux": "yarn run build && yarn run rebuild && cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder -c ./electron-builder.yml --linux",
    "rebuild": "electron-rebuild -m ./app",
    "postinstall": "electron-builder install-app-deps"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.1.0",
    "antd": "^4.12.3",
    "chalk": "^4.1.0",
    "cross-env": "^7.0.3",
    "electron": "^11.3.0",
    "electron-builder": "^22.9.1",
    "electron-rebuild": "^2.3.5",
    "esbuild": "^0.8.51",
    "react": "^17.0.0",
    "react-dom": "^17.0.0",
    "ts-node": "^9.1.1",
    "tslib": "^2.1.0",
    "typescript": "^4.1.2",
    "vite": "^2.0.1",
    "vite-plugin-imp": "^2.0.4"
  }
}
